# CLAUDE.md

## Project structure

This is a monorepo with two packages:

- `backend/` — Python pipeline: scraping, LLM filtering, CV adaptation. See `backend/CLAUDE.md` for details.
- `frontend/` — Next.js web app (in progress).
